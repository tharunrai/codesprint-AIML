# Copy this file to .env.local and fill in the values before running locally.
# Do NOT commit your real .env.local to source control.

# The deployed contract address (update after you deploy with Hardhat)
NEXT_PUBLIC_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# RPC provider for Sepolia (Infura or Alchemy). If using Infura, append your project id.
# Example: https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID
NEXT_PUBLIC_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_PROJECT_ID

# Optional: alternative RPC provider (Alchemy)
# NEXT_PUBLIC_ALCHEMY_URL=https://eth-sepolia.alchemyapi.io/v2/YOUR_ALCHEMY_KEY

# Optional: frontend feature flags or analytics keys
# NEXT_PUBLIC_SOME_KEY=

# NOTE: Private keys for contract deployment should never be stored in frontend env.
# Use the top-level contracts/.env (not committed) for Hardhat deploy PRIVATE_KEY and INFURA/ALCHEMY keys.
