"""Unit tests for core/json_utils.py.

TODO: test cases
  - stripped ```json fence → valid dict
  - prose around JSON → first { ... } block extracted
  - broken JSON (trailing comma) → repaired or clear error
  - totally invalid input → raises, no silent garbage
"""
